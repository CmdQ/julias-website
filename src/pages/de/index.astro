---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const lang = 'de';
const alternates = {
	en: '/en/',
};

const allPages = await getCollection('pages');
const entry = allPages.find(p => p.id === 'de/index.md');
if (!entry) throw new Error('Page content not found');
const { Content } = await entry.render();
---
<BaseLayout lang={lang} title="Startseite" description={entry.data.description} alternates={alternates}>
	<article class="prose">
		<Content />
	</article>
	<div class="mt-8 flex flex-wrap gap-4">
		<a class="btn-primary" href="/de/project-futopia/">
			<span class="btn-icon" aria-hidden="true">→</span>
			<span class="btn-text">Projekt Futopia</span>
		</a>
		<a class="btn-secondary" href="/de/creative-projects/">
			<span class="btn-icon" aria-hidden="true">✦</span>
			<span class="btn-text">Kreative Projekte</span>
		</a>
	</div>
</BaseLayout>
