---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const lang = 'en';
const alternates = {
	de: '/de/contact/',
};

const allPages = await getCollection('pages');
const entry = allPages.find(p => p.id === 'en/contact.md');
if (!entry) throw new Error('Page content not found');
const { Content } = await entry.render();
---
<BaseLayout lang={lang} title="Contact" description={entry.data.description} alternates={alternates}>
	<article class="prose">
		<Content />
	</article>
</BaseLayout>
